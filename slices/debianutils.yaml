package: debianutils

slices:
  bins:
    essential:
      - libc6_libs
    contents:
      /usr/bin/ischroot:
      /usr/bin/run-parts:
      /usr/bin/savelog:
      /usr/bin/tempfile:
      /usr/bin/which.debianutils:
      /usr/sbin/add-shell:
      /usr/sbin/installkernel:
      /usr/sbin/remove-shell:
      /usr/sbin/update-shells:

  config:
    essential:
      - debianutils_bins
    contents:
      /usr/share/debianutils/shells:
      /usr/share/debianutils/shells.d/: {make: true}
